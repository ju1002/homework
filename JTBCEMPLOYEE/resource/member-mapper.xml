<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>MAMEBER SQK</comment>
	<entry key="A">
		B
	</entry>
	<entry key="s">

		INSERT
		INTO
		MEMBER
		VALUES
		(
		SEQ_USERNO.NEXTVAL
		, ?
		, ?
		, ?
		, ?
		, SYSDATE
		)
	</entry>
	<entry key="findAll">
		SELECT
		E.JOB_CODE
		, SALARY
		, EMP_NAME
		, DEPT_TITLE
		, JOB_NAME
	     FROM
		EMPLOYEE E
		JOIN
		JOB J
		ON
		(J.JOB_CODE = E.JOB_CODE)
		JOIN
		DEPARTMENT D
		ON
		(D.DEPT_ID = E.DEPT_CODE)

	</entry>

	<entry key="findById">
		SELECT
		USERNO
		, USERID
		, USERPWD
		, USERNAME
		, EMAIL
		,
		ENROLLDATE
		FROM
		MEMBER
		WHERE
		USERID = ?


	</entry>

	<entry key="findByKeyword">
		SELECT
		USERNO
		, USERID
		, USERPWD
		, USERNAME
		, EMAIL
		,
		ENROLLDATE
		FROM
		MEMBER
		WHERE
		USERNAME LIKE'%'||?||'%'
	</entry>
	<entry key="update">
		UPDATE
		MEMBER
		SET
		USERPWD = ?
		WHERE
		USERID =?
		AND
		USERPWD =?
	</entry>
	<entry key="delete">
		DELETE
		FROM
		MEMBER
		WHERE
		USERID = ?
		AND
		USERPWD = ?

	</entry>

</properties>